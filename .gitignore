build
.vscode
build.log
src/version_gen.rc
src/source_link_gen.json

# outputs
out
*.nupkg

# Claude Code - ignore local settings, keep hooks and shared settings
/.claude/*
!/.claude/hooks/
!/.claude/settings.json

# fork-sync work folders
.sync/
.logs/
node_modules/

# test-harness (real-consumer smoke build) — build outputs + NuGet restore.
# Source files (main.cpp, consumer.vcxproj, run-smoke.ps1, packages.config,
# *.ps1 helpers) stay tracked.
test-harness/bin/
test-harness/obj/
test-harness/packages/
